#
# Copyright The Kubernetes Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

suite: Test controller PodDisruptionBudget

templates:
  - controller/pod_disruption_budget.yaml

release:
  name: jobset
  namespace: jobset-system

tests:
  - it: Should not create PodDisruptionBudget by default
    asserts:
      - hasDocuments:
          count: 0

  - it: Should create PodDisruptionBudget when enabled
    set:
      controller:
        podDisruptionBudget:
          enabled: true
    asserts:
      - containsDocument:
          apiVersion: policy/v1
          kind: PodDisruptionBudget
          name: jobset-controller-pdb

  - it: Should use default minAvailable value of 1
    set:
      controller:
        podDisruptionBudget:
          enabled: true
    asserts:
      - equal:
          path: spec.minAvailable
          value: 1

  - it: Should use custom minAvailable when set
    set:
      controller:
        podDisruptionBudget:
          enabled: true
          minAvailable: 2
    asserts:
      - equal:
          path: spec.minAvailable
          value: 2

  - it: Should use custom maxUnavailable when set
    set:
      controller:
        podDisruptionBudget:
          enabled: true
          maxUnavailable: 1
    asserts:
      - equal:
          path: spec.maxUnavailable
          value: 1

  - it: Should prioritize maxUnavailable over minAvailable
    set:
      controller:
        podDisruptionBudget:
          enabled: true
          minAvailable: 2
          maxUnavailable: 1
    asserts:
      - equal:
          path: spec.maxUnavailable
          value: 1
      - isNull:
          path: spec.minAvailable

  - it: Should support maxUnavailable value of 0
    set:
      controller:
        podDisruptionBudget:
          enabled: true
          maxUnavailable: 0
    asserts:
      - equal:
          path: spec.maxUnavailable
          value: 0

  - it: "Should prioritize maxUnavailable: 0 over minAvailable"
    set:
      controller:
        podDisruptionBudget:
          enabled: true
          minAvailable: 1
          maxUnavailable: 0
    asserts:
      - equal:
          path: spec.maxUnavailable
          value: 0
      - isNull:
          path: spec.minAvailable

  - it: Should support minAvailable value of 0
    set:
      controller:
        podDisruptionBudget:
          enabled: true
          minAvailable: 0
    asserts:
      - equal:
          path: spec.minAvailable
          value: 0

